import ChannelsView from "@/components/channels/ChannelsView";

export const metadata = { title: "Kanallar — AI Inbox Assistant" };

export default function ChannelsPage() {
  return <ChannelsView />;
}
