# Backend kutubxonalari. O'rnatish: pip install -r requirements.txt
fastapi            # veb-framework — API endpointlar uchun
uvicorn[standard]  # serverni ishga tushiruvchi (ASGI server)
anthropic          # Claude API — xabar tahlili uchun
httpx              # HTTP so'rovlar — Telegram API'ga xabar yuborish uchun
python-dotenv      # .env faylidan sozlamalarni o'qish
